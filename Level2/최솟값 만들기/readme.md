```py
def solution(A,B):
    answer = 0
    # 경우의 수 조합 만들기 itertool 이용해서 순열로 좌표값을 뽑는다.
    # for 루프를 두번돌려 나올수 있는 모든 경우를 실행하고 min 보다 작을때 갱신해준다.
    # 하지만 해당 방법은 너무 시간 복잡도가 높고 불필요한 경우가 많다.
    # [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
    print('Hello Python')

    return answer
```
